import { Input as UIInput } from "@/components/ui/input";
import type { InputProps as UIInputProps } from "@/components/ui/input";

export const Input = (props: UIInputProps) => <UIInput {...props} />;

export type { InputProps as UIInputProps } from "@/components/ui/input";

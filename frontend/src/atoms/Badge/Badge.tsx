import { Badge as UIBadge } from "@/components/ui/badge";
import type { BadgeProps as UIBadgeProps } from "@/components/ui/badge";

export const Badge = (props: UIBadgeProps) => <UIBadge {...props} />;

export type { BadgeProps as UIBadgeProps } from "@/components/ui/badge";

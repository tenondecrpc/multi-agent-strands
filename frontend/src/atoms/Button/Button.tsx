import { Button as UIButton } from "@/components/ui/button";
import type { ButtonProps as UIButtonProps } from "@/components/ui/button";

export const Button = (props: UIButtonProps) => <UIButton {...props} />;

export type { ButtonProps as UIButtonProps } from "@/components/ui/button";

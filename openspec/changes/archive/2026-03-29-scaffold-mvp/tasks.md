## 1. Frontend Scaffolding

- [x] 1.1 Scaffold React + Vite + TypeScript project in `frontend/` using `npm create vite@latest frontend -- --template react-ts`
- [x] 1.2 Add testing and linting tools to `frontend/package.json` (vitest, eslint) and configure them

## 2. Backend Scaffolding

- [x] 2.1 Scaffold FastAPI project structure in `backend/`
- [x] 2.2 Create `backend/requirements.txt` with fastapi, uvicorn, sqlalchemy, pydantic, ruff, pytest
- [x] 2.3 Set up basic configuration files for backend (ruff, pytest)

## 3. Infrastructure & Orchestration

- [x] 3.1 Create `.env.example` with required environment variables
- [x] 3.2 Create `docker-compose.yml` to orchestrate PostgreSQL 16, backend, and frontend
- [x] 3.3 Verify startup of all services using `docker-compose up`
